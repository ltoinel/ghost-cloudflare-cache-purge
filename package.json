{
  "private": true,
  "name": "ghost-cloudflare-cache-purge",
  "version": "1.2.0",
  "description": "A Cloudflare Worker to purge cached pages when a post is updated or created on multiple Ghost blogs",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "ltoinel <ludovic@toinel.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
